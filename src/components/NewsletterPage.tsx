const NewsletterPage = ({slug}: {slug: string}) => {
    return (
        <div>
            <h1>Newsletter page</h1>
            <p>slug: {slug}</p>
        </div>
    );
}
    
export default NewsletterPage;